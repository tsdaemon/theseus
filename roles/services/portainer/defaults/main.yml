---
portainer_enabled: false
portainer_available_externally: false

# directories
portainer_directory: "{{ docker_home }}/portainer"
portainer_data_directory: "{{ portainer_directory }}/data"

# network
portainer_port: "9000"
portainer_hostname: "portainer"
portainer_ip_allowlist: "0.0.0.0/0"

# docker
portainer_container_name: "portainer"
portainer_image_name: "portainer/portainer-ce"
portainer_image_version: "latest"

# specs
portainer_memory: 1g
