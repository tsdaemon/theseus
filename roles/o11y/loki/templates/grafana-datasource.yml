---
apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:{{ loki_http_port }}
    jsonData:
      httpHeaderName1: "X-Scope-OrgID"
    secureJsonData:
      httpHeaderValue1: "1"
